services:
  postgres:
    image: postgres:18-alpine
    container_name: momentum-e2e-postgres
    environment:
      POSTGRES_USER: momentum
      POSTGRES_PASSWORD: momentum
      POSTGRES_DB: momentum
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U momentum"]
      interval: 2s
      timeout: 5s
      retries: 10
