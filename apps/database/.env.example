# This is an example .env file for configuring the database connection.
#
# To use this, create a new file named .env in the same directory
# and replace the placeholder values with your actual database credentials.

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
