{
  "name": "db-main",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "docker-compose up -d",
    "synthesise": "tsx synthesise.ts && npx prisma format --schema=build/schema.prisma",
    "migrate": "npm run synthesise && prisma migrate dev --schema=build/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "prisma": "^6.10.1"
  },
  "devDependencies": {
    "tsx": "^4.16.2"
  }
}
